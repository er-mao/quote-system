# A simple quote_system

## Dependencies

- [Django](https://www.djangoproject.com/) = 2.0.2

## Install Django

```shell
sudo apt install python-pip
sudo pip install django
```

## Usage

```shell
python manage.py migrate
python manage.py runserver 8080
```